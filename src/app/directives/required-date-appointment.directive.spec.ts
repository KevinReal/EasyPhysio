import { RequiredDateAppointmentDirective } from './required-date-appointment.directive';

describe('RequiredDateAppointmentDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredDateAppointmentDirective();
    expect(directive).toBeTruthy();
  });
});
