import { SelectPatientFromListDirective } from './select-patient-from-list.directive';

describe('SelectPatientFromListDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectPatientFromListDirective();
    expect(directive).toBeTruthy();
  });
});
