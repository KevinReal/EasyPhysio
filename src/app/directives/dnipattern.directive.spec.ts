import { DnipatternDirective } from './dnipattern.directive';

describe('DnipatternDirective', () => {
  it('should create an instance', () => {
    const directive = new DnipatternDirective();
    expect(directive).toBeTruthy();
  });
});
