import { ShowMaxAppointmentsPipe } from './show-max-appointments.pipe';

describe('ShowMaxAppointmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowMaxAppointmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
