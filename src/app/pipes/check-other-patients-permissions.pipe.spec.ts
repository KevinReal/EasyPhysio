import { CheckOtherPatientsPermissionsPipe } from './check-other-patients-permissions.pipe';

describe('CheckOtherPatientsPermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckOtherPatientsPermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
