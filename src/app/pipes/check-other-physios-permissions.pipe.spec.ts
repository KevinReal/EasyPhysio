import { CheckOtherPhysiosPermissionsPipe } from './check-other-physios-permissions.pipe';

describe('CheckOtherPhysiosPermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckOtherPhysiosPermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
