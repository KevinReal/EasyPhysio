import { CalculateWorkHoursPipe } from './calculate-work-hours.pipe';

describe('CalculateWorkHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateWorkHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
